// miryoku/custom_macros.dtsi
// Custom ZMK macros (hyperkey, leader sequences, etc.)
// This file is intentionally empty for now - add custom macros here as needed

/ {
    macros {
        // Add custom macros here
        // Example:
        // my_macro: my_macro {
        //     compatible = "zmk,behavior-macro";
        //     #binding-cells = <0>;
        //     bindings = <&kp LSHFT &kp A>;
        // };
    };
};
